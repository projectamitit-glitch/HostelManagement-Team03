<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- PROD SERVER LOG PATH -->
    <property name="LOG_HOME" value="/logs/log_team3/prod"/>

    
    <!-- Minimal Console Logging (INFO) -->
    <!-- Disable for maximum performance -->
   
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    
    <!-- Main Rolling File Appender   -->
    
    <appender name="PROD_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/application.log</file>
        
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>20</maxHistory> 
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    
    <!-- ERROR-ONLY File Appender      -->
   
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/error.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>


    
    <!-- App Logs: Use INFO in PROD    -->
    
    <logger name="com.example.demo" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="PROD_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </logger>


    <!-- Spring Framework Logs -->
    <logger name="org.springframework" level="INFO"/>
    
    <!-- DO NOT print SQL in PROD -->
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="WARN"/>


   
    <!-- ROOT Logger: INFO only        -->
    
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="PROD_FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

</configuration>